[Unit]
Description=Emacs daemon

[Service]
Type=forking
EnvironmentFile=-/home/koral/.config/systemd/user/environment
ExecStart=/home/koral/.nix-profile/bin/emacs --daemon
ExecStop=/home/koral/.nix-profile/bin/emacsclient --eval "(progn (setq kill-emacs-hook 'nil) (kill-emacs))"
Restart=always
RestartSec=30

[Install]
WantedBy=default.target
