[Unit]
Description=Music Player Daemon

[Service]
ExecStart=/home/koral/.nix-profile/bin/mpd --no-daemon %h/.config/mpd/mpd.conf
ExecStop=/home/koral/.nix-profile/bin/mpd --kill

PIDFile=%h/.config/mpd/pid
Restart=always
RestartSec=30

[Install]
WantedBy=default.target
