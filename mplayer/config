#vo=vdpau,
#vc=ffh264vdpau,ffmpeg12vdpau,ffodivxvdpau,ffwmv3vdpau,ffvc1vdpau,


# Cache (in ko)
# cache = 8192
#
# Prefill 20% of the cache before starting playback.
#cache-min = 20.0
#
# Prefill 50% of the cache before restarting playback after the cache emptied.
#cache-seek-min = 50

# Multi-core
lavdopts = threads=4

# Subtitles
slang = en,eng,fr,fra
alang = en,fr

subfont-text-scale = 3   # Scale subtitles to X % of the screen size

#ffactor = "10"
#sub-bg-alpha="0" #background color ala closed captions
#sub-bg-color="0" #black to white
#subfont-blur="1" #Slight blur
#spuaa=20 #Lissage des sous titres DVD (?) 
